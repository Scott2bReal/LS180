-- Use SQL to determine the average, minimum, and maximum wingspan for the
-- birds shown in the table.

SELECT avg(wingspan) FROM birds;

SELECT max(wingspan) FROM birds;

SELECT min(wingspan) FROM birds;
