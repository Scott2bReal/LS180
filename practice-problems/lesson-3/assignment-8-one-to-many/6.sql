-- Why does "when" need to be quoted in many of the queries in this lesson?

/*
We need to put "when" in double quotes because it shares the name of a SQL
keyword. Double quoting it tells SQL that we are using the word "when" as a
column name, not a keyword.
*/
